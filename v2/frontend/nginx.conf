# Web container: serves the /v2 SPA and proxies /v2/api → the Rust backend.
server {
  listen 80;
  root /usr/share/nginx/html;

  location /v2/api/ {
    proxy_pass http://qs-v2-api:3010/api/;
    proxy_http_version 1.1;
    proxy_set_header Host $host;
    proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    proxy_read_timeout 300s;
    # The refresh cookie (dbdash_rt) is scoped to /api/auth by the backend, but
    # the browser sees /v2/api/auth — rewrite the cookie path so it's sent back.
    proxy_cookie_path /api/ /v2/api/;
    proxy_cookie_path /api /v2/api;
  }

  location /v2/ {
    try_files $uri $uri/ /v2/index.html;
  }

  location = /v2 { return 301 /v2/; }
}
